# 针孔太阳镜仿真 (IYPT 2027 P1) — Python 依赖
# 环境建议: Python 3.10+, RTX 5070 Ti (sm_120) 需 CUDA 12.8+ 驱动 (>=570)

# ---- 核心 (必装) ----
numpy>=1.26          # 掩膜生成、BL-ASM 传播主力
scipy>=1.11          # FFT、ndimage.label 数孔 QC、distance_transform_edt、signal.fftconvolve
matplotlib>=3.8      # 出图 (F1-F8)
pandas>=2.0          # 仿真/实验 CSV 接口 (sim/exp 按 d,p join)
scikit-image>=0.22   # regionprops 孔径分布 QC

# ---- GPU 加速 (RTX 5070 Ti 必装此版本以上) ----
# 红线: >=13.4.0 才支持 CUDA 12.8 + Blackwell sm_120, 旧版报 no kernel image
cupy-cuda12x>=13.4

# ---- 光学仿真库 ----
diffractio           # 扩展源/多波长交叉验证 (注意: 现版 GPLv3, 结果仅作对照)
diffractsim>=2.2.7   # 展示/出图, PolychromaticField 内置 CIE/D65, 支持 set_backend("CUDA")


